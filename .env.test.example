# Integration Test Environment Configuration
#
# Copy this file and customize for your testing environment.
# This file is loaded before .env for integration tests.

# Skip providers with known issues (comma-separated)
# Example: SKIP_PROVIDERS=perplexity,google
SKIP_PROVIDERS=perplexity

# Skip all real API tests (use mocks only)
# SKIP_REAL_API_TESTS=true

# Test timeout in milliseconds (default: 60000)
# TEST_TIMEOUT=60000
