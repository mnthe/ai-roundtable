# AI Roundtable - Environment Configuration
# Copy this file to .env or .env.test and fill in your API keys

# ===========================================
# AI Provider API Keys (at least one required)
# ===========================================

# Anthropic API Key (for Claude agents)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (for GPT agents)
# Get your key at: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here

# Google API Key (for Gemini agents)
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Perplexity API Key (for Perplexity agents)
# Get your key at: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# ===========================================
# Database Configuration
# ===========================================

# SQLite database file path
DATABASE_PATH=./data/roundtable.db

# ===========================================
# Test Configuration
# ===========================================

# Skip real API tests (set to 'true' to only run mock tests)
# SKIP_REAL_API_TESTS=true

# Test timeout in milliseconds (default: 60000 = 1 minute)
# TEST_TIMEOUT=60000

# ===========================================
# Agent Limits Configuration
# ===========================================

# Maximum number of persona agents per debate (default: 5)
ROUNDTABLE_MAX_AGENTS=5

# Default agent count when not specified (default: 4)
ROUNDTABLE_DEFAULT_AGENT_COUNT=4

# ===========================================
# Logging Configuration
# ===========================================

# Log level: trace, debug, info, warn, error, fatal, silent
LOG_LEVEL=info

# Log destination: stdout, file, or both
# LOG_DESTINATION=stdout

# Log file path (only used if LOG_DESTINATION includes 'file')
# LOG_FILE_PATH=./logs/ai-roundtable.log

# ===========================================
# Debug & Response Configuration
# ===========================================

# Enable verbose MCP responses (default: false)
# ROUNDTABLE_DEBUG_MODE=false

# Include full tool call details in debug mode (default: false)
# ROUNDTABLE_DEBUG_TOOL_CALLS=false

# Max length for consensus summary (default: 200, 0 = unlimited)
# ROUNDTABLE_CONSENSUS_SUMMARY_MAX_LENGTH=200

# ===========================================
# Response Quality Thresholds
# ===========================================

# Low confidence threshold for verification hints (default: 0.6)
# ROUNDTABLE_LOW_CONFIDENCE_THRESHOLD=0.6

# High/medium consensus level thresholds (default: 0.7/0.4)
# ROUNDTABLE_HIGH_CONSENSUS_THRESHOLD=0.7
# ROUNDTABLE_MEDIUM_CONSENSUS_THRESHOLD=0.4

# Confidence variance threshold for conflict detection (default: 0.04)
# ROUNDTABLE_CONFIDENCE_VARIANCE_THRESHOLD=0.04

# Minimum reasoning length ratio for depth check (default: 0.5)
# ROUNDTABLE_MIN_REASONING_LENGTH_RATIO=0.5

# Action recommendation thresholds (default: 0.8/0.6/0.5)
# ROUNDTABLE_HIGH_CONFIDENCE_ACTION_THRESHOLD=0.8
# ROUNDTABLE_LOW_CONFIDENCE_ACTION_THRESHOLD=0.6
# ROUNDTABLE_VERY_LOW_CONFIDENCE_ACTION_THRESHOLD=0.5
